# --- Frontend Dockerfile ---
FROM node:20-slim
WORKDIR /app/web
COPY web/package*.json ./
RUN npm install
COPY web/ ./
EXPOSE 5173
# Use --host to allow external access in Docker
CMD ["npm", "run", "dev", "--", "--host"]
